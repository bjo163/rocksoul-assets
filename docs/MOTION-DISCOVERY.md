# Motion discovery contract

The runtime-motion manifest supplies meaning, duration, loop policy, criticality, reduced-motion fallback, and a file-size budget for every motion kind. Explorers must render previews on a neutral checkerboard surface, expose loading/failure/fallback states as keyboard-focusable controls, and offer a compact metadata-only mode. Reduced-motion users receive the static first frame and never depend on autoplay.
